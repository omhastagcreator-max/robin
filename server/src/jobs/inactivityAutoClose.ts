// Superseded — the inactivity sweep now lives in idleAutoClose.ts as a
// single 20:00 IST cron with a 60-min threshold. This stub stays only
// because the sandbox can't delete files. Safe to remove from disk later.
export {};
