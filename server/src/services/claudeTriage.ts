// Deprecated — superseded by aiTriage.ts. Re-exporting so any stragglers
// importing from this path keep working until the next sweep.
export { triageIssue, askRobin } from './aiTriage';
export type { TriageResult } from './aiTriage';
